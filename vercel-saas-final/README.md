# LandingRodi AI — Vercel Final

## Run locally
```bash
npm install
cp .env.example .env.local
npm run dev
```

## Vercel deployment
Add these environment variables:
- `OPENAI_API_KEY`
- `OPENAI_MODEL` (optional, default `gpt-4o-mini`)
